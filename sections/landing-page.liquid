{%- if page.title != blank -%}
<div class="container">
  <div class="text-center mb-4">
    <p>{{ page.title | escape }}</p>
  </div>
</div>
{%- endif -%}
<div class="container mb-4">
  {{ page.content }}
</div>